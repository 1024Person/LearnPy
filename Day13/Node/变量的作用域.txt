当内层和外层具有同名变量的时候，会遵循就近原则

再内部函数中找同名变量的步骤：
    1、再本地找
    2、（本地没有），找上一层
    3、（上一层没有），再找上一层
    ……
    作用域：LEGB        优先级
    1、L：local               本地
    2、E:  encloseing    外层
    3、G：global             全局
    4、B：builtin             内置的

使用装饰器：@装饰器的名字
装饰器可以带参数：三层
多层装饰
