在一个函数内部在定义一个函数，
可以将每一个子函数看做是儿子辈的，最外层的函数是父亲辈的
内部函数不能改变外部标签（变量）的指向，
可变类型的变量可以改变，例如list，即使不添加golbal修饰
因为无论怎么修改内部成员，list变量(标签的指向还是不会发生改变的)
但是，如果给list1（外部变量）赋值为list2（内部变量），
那么就会报错，因为这样会改变标签的指向
所以在内部函数中可以改变可变类型的变量（标签）
所指向的内存的值，但是不能改变标签的指向

